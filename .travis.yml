language: python
python:
  - 3.6
before_install:
  - python --version
  - pip install -U pip
  - pip install -U pytest
  - pip install -U pytest-cov
  - pip install --user pylint-runner
script:
  - pytest --cov=com.ac5 com/ac5/test/
  - pylint-runner
